server:
  port: 9021
spring:
  application:
    name: aa-client
  cloud:
    config:
      uri: http://localhost:9011/
      fail-fast: true
      profile: dev
      name: client
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
